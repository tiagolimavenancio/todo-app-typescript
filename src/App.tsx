import { Routes } from 'routes';
import { Theme, GlobalStyles } from 'themes';

const App = () => {
  return (
    <Theme>
      <GlobalStyles />
      <Routes />
    </Theme>
  );
};

export default App;
